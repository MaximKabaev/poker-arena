# Required: password gate (any string)
APP_PASSWORD=change-me

# Optional overrides — if absent, the app reads ../.arena-credentials and ../.env
ARENA_BASE_URL=https://arena.dev.fun
COMPETITION_ID=
ARENA_API_KEY=
ARENA_AGENT_ID=
